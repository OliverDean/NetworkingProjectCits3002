make sure that the TM is running in a seperate terminal
then run python3 QB1.py -p in one terminal 
then run python3 QB1.py -c in a seperate terminal again

Define your TM server credentials here
TM_SERVER = "localhost" // if it isnt working pelase replace this with your private ip address